<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" >
    
  <script  type="text/javascript">
    function verifican(d){
      var numero;
      var i = 0;
    	var textfield = document.getElementById(d);
    	while (textfield.value != '' )
    	{
    	numero = textfield.value.substring(0,13);
    	document.getElementById("status").innerHTML += numero +"<br>";
    	
    	const regex = /^[1-9]\d{12}/;   //      /[1-9]\d{9}/
    	const reg = /^\s*|\s*$/g;
    		if(textfield.value.search(regex) > -1) {
    		textfield.value = textfield.value.replace(regex, "");
    		textfield.value = textfield.value.replace(reg,"");
    		i++;
        document.getElementById("numero").value = i;
        
        } 
        break;
    	}
    
    }
    </script>    

    <title>SMS, Marketing!</title>
  </head>
  <body>
    
 <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">SMS Marketer</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/contatos">Cadastro</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Ajuda</a>
      </li>
      <li class="nav-item">
        <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
      </li>
    </ul>
  </div>
</nav>
<div class="jumbotron">
  <h1 class="display-4">SMS, Massivo</h1>
  
    
    <form action="/sendsms" method="POST">
 <br></br>
  <div class="form-group">
    <label for="numero">Numero</label>
    <input type="text" name="numero" class="form-group">
  </div>

  <div class="form-group">
    <label for="numero">Texto</label>
    <textarea class="form-control" id="mensagem" name="mensagem" rows="2"></textarea>
  </div>
    <div class="form-group">
    <label for="numero">Separado Por ? </label>
    <input type="text"  name="separado" id="separado"  class="form-group">
  </div>
  <div class="form-group">
    <label for="numero">Números</label>
    <textarea class="form-control" id="numeros" name="numeros" rows="3" cols="50"></textarea>
  </div>
  <button type="submit" class="btn btn-primary">Send</button>
</form>
</div>
<div id="numerolabel">Status : {{MessageID}}</div>
<div id="status">  </div>
<br>

<table class="table table-bordered">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Nome</th>
      <th scope="col">fone</th>
      <th scope="col">Data</th>
    </tr>
  </thead>
  <tbody>
    {{#each contatos}}
    <tr>
      
      <th scope="row">{{id}}</th>
      <td>{{nome}}</td>
      <td>{{fone}}</td>
      <td></a></td>
     
    </tr>
     {{/each}}
     
     {{{msgexclusao}}}
  </tbody>
</table>
  

    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" ></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" ></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" ></script>
  </body>
</html>